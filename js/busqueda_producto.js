// const inputs = document.getElementById("buscar");
// const card = document.querySelectorAll(".card");

// const filtroBusqueda = (input, selector)=>{
//     inputs.addEventListener("keyup", e =>{
//         if(e.key==="Escape") e.target.value="";
//         document.querySelectorAll(selector).forEach(element => {
//             element.textContent.toLowerCase().includes(e.target.value) ? el.classList.remove("filter") : el.classList.add("filter")
//         })
//     })
// }
// console.log(card);
// filtroBusqueda(inputs,card);